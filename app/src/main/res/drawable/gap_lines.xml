<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="line">

    <!-- Stroke with dashes -->
    <stroke
        android:width="2dp"
        android:color="@color/whitishgrey"
        android:dashWidth="10dp"
        android:dashGap="5dp"/>
</shape>
